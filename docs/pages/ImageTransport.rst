==============
ImageTransport
==============

With the switch to Qt6, the image transport support was removed as it would require a complete rewrite
and I currently do not have the time to write and test it.
I recommend using the `ros_gst_bridge` gstreamer element in a gstreamer pipeline instead.
